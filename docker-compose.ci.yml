version: "3.4"

services:
  api:
    build:
      context: ./api
      target: development
    image: api-development
    environment:
      API_DEBUG: 1
      API_ENV: testing
      ENVIRONMENT: local
