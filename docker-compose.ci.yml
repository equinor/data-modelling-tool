version: '3.4'

services:
  api:
    build:
      context: ./api
      target: development
    entrypoint: ./wait-for-it.sh db 27017
    environment:
      FLASK_ENV: development
      ENVIRONMENT: local
      MONGO_INITDB_ROOT_PASSWORD: maf

  db:
    environment:
       MONGO_INITDB_ROOT_PASSWORD: maf
