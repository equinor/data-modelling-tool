export enum Status {
    STARTING = 'starting',
    RUNNING = 'running',
    FAILED = 'failed',
    COMPLETED = 'completed',
    UNKNOWN = 'unknown',
}

export enum Blueprints {
    ANALYSIS = 'AnalysisPlatformDS/Blueprints/Analysis',
    BLOB = 'system/SIMOS/Blob',
}
