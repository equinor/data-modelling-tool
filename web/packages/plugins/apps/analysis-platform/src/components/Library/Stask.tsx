import React from 'react'
import { DmtSettings } from '../../Types'

const Stask = (props: DmtSettings): JSX.Element => {
  const { settings } = props

  return <>Stask placeholder</>
}

export default Stask
