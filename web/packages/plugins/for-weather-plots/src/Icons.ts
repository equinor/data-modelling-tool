import { Icon } from '@equinor/eds-core-react'
import { save, close, add } from '@equinor/eds-icons'

Icon.add({
  save,
  close,
  add,
})

export default Icon
// Usage example:
// <Icon name="info_circle" size={24} />
