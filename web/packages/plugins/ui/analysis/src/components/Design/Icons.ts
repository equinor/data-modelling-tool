import { Icon } from '@equinor/eds-core-react'
import {
  add,
  assignment_user,
  close,
  edit_text,
  time,
} from '@equinor/eds-icons'

Icon.add({
  add,
  assignment_user,
  close,
  edit_text,
  time,
})

export default Icon
