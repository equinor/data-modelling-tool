import parent from './blueprint_test.json'
import blueprint from './car_test.json'
import { findRecipe } from '../../../pluginUtils'
import { generateUiSchema } from '../GenerateUiSchema'

describe.skip('Generate uiSchema', () => {
  it('should generate uiSchema for a car', () => {
    // const uiSchema = generateUiSchema({blueprint, document, children: [], name: 'EDIT'}, 'EDIT')
    // expect((uiSchema as any)['description']).toEqual({
    //   'ui:widget': 'textarea',
    // })
    // expect((uiSchema as any)['uiRecipes']).toEqual({ 'ui:field': 'hidden' })
    // expect((uiSchema as any)['storageRecipes']).toEqual({
    //   'ui:field': 'hidden',
    // })
    // console.log(uiSchema)
  })
})
